# Três instâncias de API + Postgres — concorrência e exclusão mútua
name: sd04-concorrencia-postgres

services:
  postgres:
    # bitnami/postgresql:16 sumiu do Hub; lab simples → postgres oficial
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: portal
      POSTGRES_PASSWORD: portal
      POSTGRES_DB: portal
    volumes:
      - ./primary/init:/docker-entrypoint-initdb.d:ro,Z
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5438:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U portal -d portal"]
      interval: 5s
      timeout: 5s
      retries: 20

  api1:
    build: ./api
    environment:
      PRIMARY_DSN: host=postgres port=5432 dbname=portal user=portal password=portal
      INSTANCE_ID: api-1
      RACE_DELAY_MS: "150"
    depends_on:
      postgres:
        condition: service_healthy

  api2:
    build: ./api
    environment:
      PRIMARY_DSN: host=postgres port=5432 dbname=portal user=portal password=portal
      INSTANCE_ID: api-2
      RACE_DELAY_MS: "150"
    depends_on:
      postgres:
        condition: service_healthy

  api3:
    build: ./api
    environment:
      PRIMARY_DSN: host=postgres port=5432 dbname=portal user=portal password=portal
      INSTANCE_ID: api-3
      RACE_DELAY_MS: "150"
    depends_on:
      postgres:
        condition: service_healthy

  nginx:
    image: nginx:alpine
    ports:
      - "8087:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro,Z
    depends_on:
      - api1
      - api2
      - api3

volumes:
  postgres_data:
