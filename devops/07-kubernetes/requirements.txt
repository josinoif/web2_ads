# Requirements consolidados do Módulo 7 — Kubernetes
# Instalar com: pip install -r requirements.txt
#
# Recomendação: ambiente virtual isolado para este módulo.
#   python3 -m venv .venv && source .venv/bin/activate

# --- Core ---
pytest>=8.0

# --- Cliente oficial do Kubernetes para Python (scripts de apoio) ---
kubernetes>=30.0

# --- App exemplo (FastAPI + Postgres + Redis) ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
redis>=5.0
psycopg[binary]>=3.2

# --- Scripts de apoio ---
pyyaml>=6.0
rich>=13.0
httpx>=0.27

# --- Opcional: testes de integração contra cluster local ---
# kind ou k3d devem estar instalados no host
